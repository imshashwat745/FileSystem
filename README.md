# FileSystem
This is a C++ project whcih creates directories and text files with version control on file content, and executes in RAM

Open in any c++ editor for the code
run to run the code directly
